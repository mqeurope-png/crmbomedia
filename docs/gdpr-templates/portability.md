# Plantilla — Respuesta a solicitud de portabilidad (art. 20 RGPD)

**Asunto:** Entrega de sus datos en formato portable

Estimado/a {{NOMBRE_TITULAR}},

En respuesta a su solicitud del {{FECHA_SOLICITUD}}, y en ejercicio del
**derecho a la portabilidad** previsto en el artículo 20 del RGPD,
{{RAZON_SOCIAL}} le hace entrega de los datos personales que usted nos
proporcionó, en un **formato estructurado, de uso común y lectura
mecánica**.

Adjuntamos dos ficheros con el mismo contenido:

- `{{FILENAME_JSON}}` — JSON estructurado, recomendado para sistemas
  informáticos.
- `{{FILENAME_CSV}}` — CSV plano con columna `section` indicando el tipo
  de registro (contacto, nota, tarea, referencia externa, auditoría).

Si lo desea, podemos transmitir directamente estos datos a otro
responsable del tratamiento, siempre que sea técnicamente posible.
Indíquenos los datos de destino (responsable + contacto) y procederemos
en el plazo de un mes desde su confirmación.

Le recordamos que la portabilidad cubre los datos que usted nos
proporcionó directamente y se trate de forma automatizada con base en su
consentimiento o un contrato. No incluye los datos derivados del
tratamiento (analíticas, segmentaciones internas).

Atentamente,

{{NOMBRE_OPERADOR}}
{{CARGO}}
{{EMAIL_CONTACTO_DPO}}

---

**Datos internos (no enviar al titular):**

- ID solicitud CRM: `{{REQUEST_ID}}`
- Fichero JSON: `{{EVIDENCE_PATH_JSON}}`
- Fichero CSV: `{{EVIDENCE_PATH_CSV}}`
- Procesada por: `{{ADMIN_EMAIL}}`
- Fecha proceso: `{{COMPLETED_AT}}`
